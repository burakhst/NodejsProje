<%- include('./partials/header')%>
<title>Dgs Konuları</title>
<div class="container" style="width: auto; vertical-align:center; display:table;">
    <div class="row">
        <% veri.forEach(function(kayıt) { %>
        <a href="/dgsKonu/<%= kayıt.Id %>">
            <div
                style="width: 400px; padding: 10px; margin:10px; border: 1px solid teal; float: left; text-align: center;">
                <div style="padding-left:5px;"><%= kayıt.dersAdi %></div>
            </div>
        </a>
        <% }); %>
    </div>
</div>
